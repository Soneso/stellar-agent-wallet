version = 1
scope = "profile:*"

[[rules]]
match = { tool = "stellar_pay", chain = "*" }
criteria = [
    { kind = "per_period_cap", asset = "native", window = "1d", max_stroops = 50_000_000_000 },
]
decision = "allow"
