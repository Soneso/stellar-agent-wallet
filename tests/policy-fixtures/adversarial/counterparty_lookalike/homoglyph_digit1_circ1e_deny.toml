version = 1
scope = "profile:*"

[[rules]]
match = { tool = "stellar_pay", chain = "*" }
criteria = [
    { kind = "counterparty_allowlist", kinds = ["HOME_DOMAIN"], allowlist = ["circle.com"] },
]
decision = "allow"
